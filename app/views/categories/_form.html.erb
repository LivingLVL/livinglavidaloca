<%= form_with(model: category, local: true) do |form| %>
  <% if category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(category.errors.count, "error") %> prohibited this category from being saved:</h2>

      <ul>
      <% category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    
    <%= form.text_field :title, class: "form-control form-control1", placeholder: "Title", required: true %>
  </div>

  <div class="form-group">
    
    <%= form.text_area :description, class: "form-control form-control1", placeholder: "Description", required: true %>
  </div>

  <div class="form-group">
    
    <%= form.text_field :image, class: "form-control form-control1", placeholder: "Image" %>
  </div>

  <div class="actions">
    <%= form.submit "Send", class: "smallbutton" %>
  </div>
<% end %>
